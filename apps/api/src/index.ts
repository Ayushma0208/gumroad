/**
 * Express API will live here in the next milestone.
 *
 * Planned modules (feature folders, clean modular monolith):
 *   auth, users, creators, products, categories, cart, orders, payments, admin
 *
 * Each module:
 *   module.routes.ts → module.controller.ts → module.service.ts
 *   module.schema.ts (Zod) · module.types.ts
 */
export {};
